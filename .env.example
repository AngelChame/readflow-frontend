# URL base del backend de ReadFlow
# Ejemplo local: http://localhost:8080/api/v1
EXTERNAL_API_URL=https://api.example.com/api/v1

# Token de autenticación para las rutas de administración
# Debe coincidir con el token configurado en el backend
ADMIN_TOKEN=your_admin_token_here
